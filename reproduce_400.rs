use chitti::brains::gemini::types::*;
use serde_json::json;

fn main() 
    let request = InteractionRequest 
        model: Some(gemini-3-flash-preview.to_string());
    println!(Untagged Text Input: {}
 use chitti::brains::gemini::types::*;
use serde_json::json;

fn main() 
    let request = InteractionRequest 
        input: InteractionInput::Text(Hello.to_string());
    println!(Untagged Text Input: {}
 use chitti::brains::gemini::types::*;
use serde_json::json;

fn main() 
    let request = InteractionRequest 
        ..Default::default()
    ;
    println!(Untagged Text Input: {}
 use chitti::brains::gemini::types::*;
use serde_json::json;

fn main()  serde_json::to_string_pretty(&request).unwrap());

    let turn = InteractionTurn 
        role: Role::User;
    let request2 = InteractionRequest 
        model: Some(gemini-3-flash-preview.to_string());
    println!(Turns Input: {}
 use chitti::brains::gemini::types::*;
use serde_json::json;

fn main()  serde_json::to_string_pretty(&request).unwrap());

    let turn = InteractionTurn 
        role: Role::User;
    let request2 = InteractionRequest 
        input: InteractionInput::Turns(vec![turn]);
    println!(Turns Input: {}
 use chitti::brains::gemini::types::*;
use serde_json::json;

fn main()  serde_json::to_string_pretty(&request).unwrap());

    let turn = InteractionTurn 
        role: Role::User;
    let request2 = InteractionRequest 
        ..Default::default()
    ;
    println!(Turns Input: {}
 use chitti::brains::gemini::types::*;
use serde_json::json;

fn main()  serde_json::to_string_pretty(&request).unwrap());

    let turn = InteractionTurn 
        content: InteractionContent::from(Hello.to_string());
    let request2 = InteractionRequest 
        model: Some(gemini-3-flash-preview.to_string());
    println!(Turns Input: {}
 use chitti::brains::gemini::types::*;
use serde_json::json;

fn main()  serde_json::to_string_pretty(&request).unwrap());

    let turn = InteractionTurn 
        content: InteractionContent::from(Hello.to_string());
    let request2 = InteractionRequest 
        input: InteractionInput::Turns(vec![turn]);
    println!(Turns Input: {}
 use chitti::brains::gemini::types::*;
use serde_json::json;

fn main()  serde_json::to_string_pretty(&request).unwrap());

    let turn = InteractionTurn 
        content: InteractionContent::from(Hello.to_string());
    let request2 = InteractionRequest 
        ..Default::default()
    ;
    println!(Turns Input: {}
 use chitti::brains::gemini::types::*;
use serde_json::json;

fn main()  serde_json::to_string_pretty(&request).unwrap());

    let turn = InteractionTurn 
    ;
    let request2 = InteractionRequest 
        model: Some(gemini-3-flash-preview.to_string());
    println!(Turns Input: {}
 use chitti::brains::gemini::types::*;
use serde_json::json;

fn main()  serde_json::to_string_pretty(&request).unwrap());

    let turn = InteractionTurn 
    ;
    let request2 = InteractionRequest 
        input: InteractionInput::Turns(vec![turn]);
    println!(Turns Input: {}
 use chitti::brains::gemini::types::*;
use serde_json::json;

fn main()  serde_json::to_string_pretty(&request).unwrap());

    let turn = InteractionTurn 
    ;
    let request2 = InteractionRequest 
        ..Default::default()
    ;
    println!(Turns Input: {}
 use chitti::brains::gemini::types::*;
use serde_json::json;

fn main()  serde_json::to_string_pretty(&request2).unwrap());

